# Sounds

PCM audio assets embedded into the ESP32 firmware by `main/CMakeLists.txt`.

The firmware uses these files for:

- message notification sound
- animal quiz sounds
- alarm playback

Keep files small because they are embedded into the firmware image.
