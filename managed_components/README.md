# Managed Components

ESP-IDF Component Manager dependencies checked into this workspace.

- `espressif__mqtt/` provides the MQTT client used by the firmware.
- `espressif__esp-nn/` provides optimized neural-network kernels pulled by the
  component manager.

These are third-party/generated dependencies. Do not edit them for normal app
changes; update them through ESP-IDF Component Manager when needed.
