# Patches

Contains project patches for third-party components.

- `lvgl_esp32_drivers_8-3.patch` adapts the ESP32 LVGL driver code for the LVGL
  version and display setup used by this project.

Use `scripts/apply_patch.sh` on macOS/Linux or `scripts/apply_patch.ps1` on
Windows to apply it.
